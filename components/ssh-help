ROFI: SSH

1. Look up the file formats in ~/.ssh/config and copy that information. 

***
The ~/.ssh directory is automatically created when the user runs the ssh command for the first time. In its simplest form, it contains one or more entries with the remote user name, hostname, and port. (For example: john@dev.example.com -p 2322.).
***

2. In a terminal open a ssh function (modi) in rofi with the command:

rofi -show ssh  <<-- or define a permanent function in ~/.config/rofi/config.rasi

Paste the copied information at the top right after the "ssh" prompt. If the connection has a password you will next need to enter it, unless you have set that up in ~/.ssh/config


*****DE*****
1. Schlagen Sie die Dateiformate in ~/.ssh/config nach und kopieren Sie diese Informationen. 

***
Das Verzeichnis ~/.ssh wird automatisch erstellt, wenn der Benutzer den Befehl ssh zum ersten Mal ausführt. In seiner einfachsten Form enthält es einen oder mehrere Einträge mit dem Namen des entfernten Benutzers, dem Hostnamen und dem Port. (Zum Beispiel: john@dev.example.com -p 2322.).
***

2. Öffnen Sie in einem Terminal eine ssh-Funktion (modi) in rofi mit dem Befehl:

rofi -show ssh <<-- oder definieren Sie eine permanente Funktion in ~/.config/rofi/config.rasi

Fügen Sie die kopierten Informationen oben rechts nach der "ssh"-Eingabeaufforderung ein. Wenn die Verbindung ein Passwort hat, müssen Sie es als nächstes eingeben, es sei denn, Sie haben es in ~/.ssh/config festgelegt


*****ES*****
1. Busca los formatos de archivo en ~/.ssh/config y copia esa información. 

***
El directorio ~/.ssh se crea automáticamente cuando el usuario ejecuta el comando ssh por primera vez. En su forma más simple, contiene una o más entradas con el nombre del usuario remoto, el nombre del host y el puerto. (Por ejemplo: john@dev.example.com -p 2322.).
***

2. En un terminal abre una función ssh (modi) en rofi con el comando

rofi -show ssh <<-- o define una función permanente en ~/.config/rofi/config.rasi

Pega la información copiada en la parte superior derecha después del prompt "ssh". Si la conexión tiene una contraseña, deberá introducirla a continuación, a menos que la haya configurado en ~/.ssh/config


*****EL*****
1. Αναζητήστε τις μορφές αρχείων στο ~/.ssh/config και αντιγράψτε τις πληροφορίες αυτές. 

***
Ο κατάλογος ~/.ssh δημιουργείται αυτόματα όταν ο χρήστης εκτελεί την εντολή ssh για πρώτη φορά. Στην απλούστερη μορφή του, περιέχει μία ή περισσότερες καταχωρήσεις με το όνομα του απομακρυσμένου χρήστη, το όνομα κεντρικού υπολογιστή και τη θύρα. (Για παράδειγμα: john@dev.example.com -p 2322.).
***

2. Σε ένα τερματικό ανοίξτε μια λειτουργία ssh (modi) στο rofi με την εντολή:

rofi -show ssh <<-- ή ορίστε μια μόνιμη λειτουργία στο ~/.config/rofi/config.rasi

Επικολλήστε τις πληροφορίες που αντιγράψατε στο πάνω μέρος δεξιά μετά την προτροπή "ssh". Αν η σύνδεση έχει κωδικό πρόσβασης, θα πρέπει στη συνέχεια να τον εισαγάγετε, εκτός αν τον έχετε ορίσει στο ~/.ssh/config


*****FR*****
1. Recherchez les formats de fichiers dans ~/.ssh/config et copiez ces informations. 

***
Le répertoire ~/.ssh est automatiquement créé lorsque l'utilisateur exécute la commande ssh pour la première fois. Dans sa forme la plus simple, il contient une ou plusieurs entrées avec le nom d'utilisateur distant, le nom d'hôte et le port. (Par exemple : john@dev.example.com -p 2322.).
***

2. Dans un terminal, ouvrez une fonction ssh (modi) dans rofi avec la commande :

rofi -show ssh <<-- ou définissez une fonction permanente dans ~/.config/rofi/config.rasi.

Collez les informations copiées en haut à droite après l'invite "ssh". Si la connexion a un mot de passe, vous devrez ensuite le saisir, à moins que vous ne l'ayez configuré dans ~/.ssh/config.


*****IT*****
1. Cercare i formati dei file in ~/.ssh/config e copiare queste informazioni. 

***
La directory ~/.ssh viene creata automaticamente quando l'utente esegue il comando ssh per la prima volta. Nella sua forma più semplice, contiene una o più voci con il nome dell'utente remoto, il nome host e la porta. (Ad esempio: john@dev.example.com -p 2322).
***

2. In un terminale aprire una funzione ssh (modi) in rofi con il comando:

rofi -show ssh <<-- oppure definire una funzione permanente in ~/.config/rofi/config.rasi

Incollare le informazioni copiate in alto a destra dopo il prompt "ssh". Se la connessione ha una password, sarà necessario inserirla, a meno che non sia stata impostata in ~/.ssh/config


*****JA*****
1. ~/.ssh/config でファイル形式を調べ、その情報をコピーする。

***
~/.ssh ディレクトリは、ユーザが初めて ssh コマンドを実行したときに自動的に作成されま す。最も単純な形式では、リモートユーザ名、ホスト名、およびポートを含む1つまたは複数のエントリが含まれています。(例: john@dev.example.com -p 2322).
***

2. ターミナルでrofiのssh機能(modi)をコマンドで開く。

rofi -show ssh <<-- または ~/.config/rofi/config.rasi に恒久的な関数を定義する。

コピーした情報を "ssh "プロンプトの右上に貼り付けてください。接続にパスワードがある場合、~/.ssh/config で設定していない限り、次にそれを入力する必要があります。


*****PL*****
1. Sprawdź formaty plików w ~/.ssh/config i skopiuj te informacje. 

***
Katalog ~/.ssh jest tworzony automatycznie, gdy użytkownik po raz pierwszy uruchamia polecenie ssh. W najprostszej postaci zawiera on jeden lub więcej wpisów z nazwą zdalnego użytkownika, nazwą hosta i portem. (Na przykład: john@dev.example.com -p 2322.)
***

2. W terminalu otwórz funkcję ssh (modi) w rofi za pomocą polecenia:

rofi -show ssh <<-- lub zdefiniuj stałą funkcję w ~/.config/rofi/config.rasi.

Skopiowane informacje wklej u góry po prawej stronie po znaku "ssh". Jeśli połączenie ma hasło, będziesz musiał je podać, chyba że ustawiłeś je w ~/.ssh/config


*****PT*****
1. Procure os formatos dos ficheiros em ~/.ssh/config e copie essa informação. 

***
O directório ~/.ssh é automaticamente criado quando o utilizador executa o comando ssh pela primeira vez. Na sua forma mais simples, contém uma ou mais entradas com o nome do utilizador remoto, nome da máquina, e porta. (Por exemplo: john@dev.example.com -p 2322.).
***

2. Num terminal, abrir uma função ssh (modi) em rofi com o comando:

rofi -show ssh <<<-- ou definir uma função permanente em ~/.config/rofi/config.rasi

Colar a informação copiada na parte superior direita após a solicitação "ssh". Se a ligação tiver uma palavra-passe, terá de a introduzir a seguir, a menos que tenha configurado isso em ~/.ssh/config


*****RU*****
1. Найдите форматы файлов в ~/.ssh/config и скопируйте эту информацию. 

***
Каталог ~/.ssh создается автоматически, когда пользователь впервые выполняет команду ssh. В своей простейшей форме он содержит одну или несколько записей с именем удаленного пользователя, именем хоста и портом. (Например: john@dev.example.com -p 2322.).
***

2. В терминале откройте функцию ssh (modi) в rofi командой:

rofi -show ssh <<-- или определите постоянную функцию в ~/.config/rofi/config.rasi.

Вставьте скопированную информацию вверху справа после приглашения "ssh". Если соединение имеет пароль, вам нужно будет ввести его, если вы не задали его в ~/.ssh/config.


*****SV*****
1. Slå upp filformaten i ~/.ssh/config och kopiera informationen. 

***
Katalogen ~/.ssh skapas automatiskt när användaren kör kommandot ssh för första gången. I sin enklaste form innehåller den en eller flera poster med fjärranvändarnamn, värdnamn och port. (Till exempel: john@dev.example.com -p 2322.).
***

2. I en terminal öppnar du en ssh-funktion (modi) i rofi med kommandot:

rofi -show ssh <<-- eller definiera en permanent funktion i ~/.config/rofi/config.rasi

Klistra in den kopierade informationen uppe till höger efter "ssh"-prompten. Om anslutningen har ett lösenord måste du nu ange det, om du inte har ställt in det i ~/.ssh/config

*****ZH*****
1. 在~/.ssh/config中查找文件格式并复制该信息。

***
当用户第一次运行ssh命令时，~/.ssh目录会自动创建。在其最简单的形式中，它包含了一个或多个带有远程用户名、主机名和端口的条目。(例如：john@dev.example.com -p 2322。)。
***

2. 在终端中用命令打开rofi中的ssh功能（modi）。

rofi -show ssh <<-- 或者在~/.config/rofi/config.rasi中定义一个永久的函数

将复制的信息粘贴在 "ssh "提示后的右上方。如果连接有密码，你接下来需要输入密码，除非你已经在~/.ssh/config中设置了密码。

***************************************************
Translations by https://www.deepl.com/en/translator
***************************************************
