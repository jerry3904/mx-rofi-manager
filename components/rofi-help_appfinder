X=
=
Z=

Appfinder (mxfb-appfinder)

TAB ICONS

 All apps - shows all apps
 Categories - filters apps according to the the selected category
 Help - this document

CUSTOMIZATION

Appfinder uses a unique pair of configuration files that are located in ~/.config/rofi/themes/appfinder.

1. mxfb-appfinder-config.rasi: 
--the "window" section sets position and size
--the "color" section sets colors
2. mxfb-appfinder.rasi: 
--sets which tabs are displayed
--imports other settings from the file above.

Both files can be manually edited with a text editor such as Featherpad.


*****DE*****
TAB-SYMBOLE

 Alle Apps - zeigt alle Apps an
 Kategorien - filtert Apps nach der ausgewählten Kategorie
 Hilfe - dieses Dokument

ANPASSUNG

Appfinder verwendet ein einzigartiges Paar von Konfigurationsdateien, die sich in ~/.config/rofi/themes/appfinder befinden.

1. mxfb-appfinder-config.rasi: 
-der Abschnitt "window" legt Position und Größe fest
-der Abschnitt "color" legt die Farben fest
2. mxfb-appfinder.rasi: 
-Legt fest, welche Registerkarten angezeigt werden
-importiert andere Einstellungen aus der obigen Datei.

Beide Dateien können manuell mit einem Texteditor wie z.B. Featherpad bearbeitet werden.

*****ES*****
ICONOS DE LA PESTAÑA

 Todas las apps - muestra todas las apps
 Categorías - filtra las aplicaciones según la categoría seleccionada
 Ayuda - este documento

PERSONALIZACIÓN

Appfinder utiliza un par único de archivos de configuración que se encuentran en ~/.config/rofi/themes/appfinder.

1. mxfb-appfinder-config.rasi: 
--la sección "ventana" establece la posición y el tamaño
--la sección "color" establece los colores
2. mxfb-appfinder.rasi: 
--establece qué pestañas se muestran
--importa otros ajustes del archivo anterior.

Ambos archivos pueden editarse manualmente con un editor de texto como Featherpad.

*****EL*****
ΤΑΜΠ ICONS

 Όλες οι εφαρμογές - εμφανίζει όλες τις εφαρμογές
 Κατηγορίες - φιλτράρει τις εφαρμογές σύμφωνα με την επιλεγμένη κατηγορία
 Βοήθεια - αυτό το έγγραφο

ΠΡΟΣΑΡΜΟΓΗ

Το Appfinder χρησιμοποιεί ένα μοναδικό ζεύγος αρχείων διαμόρφωσης που βρίσκονται στο ~/.config/rofi/themes/appfinder.

1. mxfb-appfinder-config.rasi: 
-το τμήμα "window" ορίζει τη θέση και το μέγεθος
-το τμήμα "color" ορίζει τα χρώματα
2. mxfb-appfinder.rasi: 
-ορίζει ποιες καρτέλες εμφανίζονται
--μεταφέρει άλλες ρυθμίσεις από το παραπάνω αρχείο.

Και τα δύο αρχεία μπορούν να επεξεργαστούν χειροκίνητα με έναν επεξεργαστή κειμένου όπως το Featherpad.

*****FR*****
TAB ICONS

 Toutes les applications - affiche toutes les applications
 Catégories - filtre les applications en fonction de la catégorie sélectionnée
 Aide - ce document

PERSONNALISATION

Appfinder utilise une paire unique de fichiers de configuration qui sont situés dans ~/.config/rofi/themes/appfinder.

1. mxfb-appfinder-config.rasi : 
--la section "window" définit la position et la taille de la fenêtre
--la section "color" définit les couleurs
2. mxfb-appfinder.rasi : 
--détermine les onglets à afficher
--importe d'autres paramètres du fichier ci-dessus.

Les deu fichiers peuvent être édités manuellement avec un éditeur de texte tel que Featherpad.


*****IT*****
TAB ICONE

 Tutte le app - mostra tutte le app
 Categorie - filtra le app in base alla categoria selezionata
 Aiuto - questo documento

PERSONALIZZAZIONE

Appfinder utilizza una coppia unica di file di configurazione che si trovano in ~/.config/rofi/themes/appfinder.

1. mxfb-appfinder-config.rasi: 
-la sezione "finestra" imposta la posizione e le dimensioni
-la sezione "colore" imposta i colori
2. mxfb-appfinder.rasi: 
-imposta quali schede vengono visualizzate
--importa altre impostazioni dal file precedente.

Entrambi i file possono essere modificati manualmente con un editor di testo come Featherpad.

*****JA*****
TAB ICONS

 All apps - すべてのアプリを表示します。
 Categories - 選択したカテゴリに従ってアプリをフィルタリングします。
 Help - このドキュメントを表示します。

カスタマイズ

Appfinderは、~/.config/rofi/themes/appfinderにある独自の設定ファイルのペアを使用します。

1. mxfb-appfinder-config.rasi： 
-"window "セクションは、位置と大きさを設定します。
--color "セクションは色を設定します。
2. mxfb-appfinder.rasi： 
-どのタブを表示するか設定します
--imported other settings from the file above.

どちらのファイルも、Featherpadなどのテキストエディタで手動で編集することができます。

*****PL*****
TAB ICONS

 Wszystkie aplikacje - wyświetla wszystkie aplikacje
 Kategorie - filtruje aplikacje według wybranej kategorii
 Pomoc - ten dokument

DOSTOSOWYWANIE

Appfinder używa unikalnej pary plików konfiguracyjnych, które znajdują się w ~/.config/rofi/themes/appfinder.

1. mxfb-appfinder-config.rasi: 
--sekcja "window" ustawia pozycję i rozmiar
--sekcja "color" ustawia kolory
2. mxfb-appfinder.rasi: 
-ustawia, które zakładki są wyświetlane
--importuje inne ustawienia z powyższego pliku.

Oba pliki można edytować ręcznie za pomocą edytora tekstu, takiego jak Featherpad. 

*****PT*****
ÍCONES DO SEPARADOR

 Todas as aplicações - mostra todas as aplicações
 Categorias - filtra as aplicações de acordo com a categoria seleccionada
 Ajuda - este documento

PERSONALIZAÇÃO

O Appfinder utiliza um par único de ficheiros de configuração que estão localizados em ~/.config/rofi/themes/appfinder.

1. mxfb-appfinder-config.rasi: 
--a secção "window" define a posição e o tamanho
-a secção "color" define as cores
2. mxfb-appfinder.rasi: 
--define que separadores são mostrados
--importa outras definições do ficheiro acima.

Ambos os ficheiros podem ser editados manualmente com um editor de texto como o Featherpad.

*****RU*****
ЗНАЧКИ ТАБОВ

 Все приложения - показывает все приложения
 Категории - фильтрует приложения в соответствии с выбранной категорией
 Справка - этот документ

КАСТОМИЗАЦИЯ

Appfinder использует уникальную пару конфигурационных файлов, которые находятся в ~/.config/rofi/themes/appfinder.

1. mxfb-appfinder-config.rasi: 
-секция "window" устанавливает положение и размер.
-секция "color" устанавливает цвета
2. mxfb-appfinder.rasi: 
-устанавливает, какие вкладки будут отображаться
-импортирует другие настройки из файла выше.

Оба файла могут быть отредактированы вручную с помощью текстового редактора, такого как Featherpad.

*****SV*****
TAB ICONS

 Alla appar - visar alla appar
 Kategorier - filtrerar appar enligt den valda kategorin
 Hjälp - det här dokumentet

ANPASSNING

Appfinder använder ett unikt par konfigurationsfiler som finns i ~/.config/rofi/themes/appfinder.

1. mxfb-appfinder-config.rasi: 
--avsnittet "window" anger position och storlek.
-avsnittet "color" ställer in färger
2. mxfb-appfinder.rasi: 
--inställer vilka flikar som visas
--importerar andra inställningar från filen ovan.

Båda filerna kan redigeras manuellt med en textredigerare som Featherpad.

*****ZH*****
选项卡 ICONS

 所有应用程序 - 显示所有应用程序
 类别 - 根据所选类别过滤应用程序
 帮助 - 本文档

定制

Appfinder使用一对独特的配置文件，这些文件位于~/.config/rofi/themes/appfinder。

1. mxfb-appfinder-config.rasi： 
--窗口 "部分设置位置和大小
--"颜色 "部分设置颜色
2. mxfb-appfinder.rasi： 
--设置哪些标签被显示
--从上面的文件中导入其他设置。

这两个文件都可以用文本编辑器（如Featherpad）进行手动编辑。


Translated with www.DeepL.com/Translator (free version)
